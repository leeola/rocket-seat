''''''

# Standard
# Related
# Local

css_files = (
    # A single CSS file, no special options
    'blueprint/screen.css',
    # A tuple defined for options, in the order of:
    # 0: css filename
    # 1: media type (leave empty string if default)
    # 2: IE Only
    ('blueprint/print.css', 'print'),
    ('blueprint/ie.css', '', True),
    'layout.css',
    'style.css',
)

humane_name = None

regions = (
)

page_templates = (
    'page.html',
)

region_templates = (
)

block_templates = (
)
